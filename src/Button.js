import React, {Component} from 'react';

class Button extends Component {
    render() {
        return (
            <button
                type='submit'
            >
                Search
            </button>
        );
    }
}

export default Button;